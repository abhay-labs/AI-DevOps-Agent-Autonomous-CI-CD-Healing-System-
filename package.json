{
  "name": "alpha-devops-agent",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && rift\\Scripts\\python -m uvicorn main:app --reload --reload-exclude workspaces",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}